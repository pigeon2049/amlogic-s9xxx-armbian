[Service]
# Missing keys must be repaired before sshd -t, even with an empty machine-id
# or after an interrupted first boot. ssh-keygen -A preserves existing keys.
ExecStartPre=
ExecStartPre=/usr/bin/ssh-keygen -A
ExecStartPre=/usr/sbin/sshd -t
