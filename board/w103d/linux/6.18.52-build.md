# Linux 6.18.52-ophub build record (production)

Board-tested production kernel for the 6.18.52 server line
(`W103D_Armbian_26.8.1_6.18.52_Server_MainlineUboot.burn.img`,
2026-09-20). Supersedes the 6.18.49 validation for new builds;
6.18.49 images are retained only as re-flash rollback.

## Sources

- `https://github.com/ophub/linux-6.18.y` @
  `2e5b348be5ac629ba8a8cffbd710a19acaa43787` (6.18.52)
- Config: `ophub/kernel` `config-6.18` @ `fad77129` layered with
  `board/w103d/linux/w103d-6.18.config` (this branch; unchanged since
  the 6.18.52 build)
- Port: `board/w103d/linux/6.18.52-port.diff` (8 files vs upstream HEAD):
  fork probe fix + console silence (`mt7663s_w103d.c`, `mt7663s_txrx.c`,
  `sdio.c`, `btmtksdio-w103d.h`, `btmtksdio.c`, `mmc/core/sdio.c`,
  `meson-gx-mmc.c`) and fork `0007` mac80211 rate-reporting patch
  (`net/mac80211/tx.c`, applied cleanly). DTS is upstream as-is.
- Toolchain: Ubuntu 24.04 `gcc-aarch64-linux-gnu` 13.3.0, pahole 1.25
- Command: `ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu-
  LOCALVERSION=-ophub` (the `-ophub` suffix is mandatory for on-board
  modules; anything else is an invalid deployment artifact)

## Artifacts (SHA-256)

- Image `17fe3fdb…6c6ed` (48,396,800 bytes); DTB `575941ed…357129`
  (byte-identical to the tested 6.18.49 DTB)
- `mt7663s.ko` `b7f90324…` (build-id `ba20eeb2…`),
  `btmtksdio.ko` `d2684006…`, `mac80211.ko` `e866385a…`
- vermagic first field strictly `6.18.52-ophub`; `mt7663s` depends
  `mt76-connac-lib,btmtksdio,mt7663-usb-sdio-common,mt7615-common,mt76-sdio,mt76,mac80211,cfg80211`
  with alias `sdio:c*v037Ad7603*`
- Firmware pinned: `mt7663_n9_v3.bin` `223f73f1…`,
  `mt7663pr2h.bin` `534f2152…` (dpkg-divert protected)

## Validation

`board/w103d/validation/build-kernel.sh` checks apply cleanly;
`verify_image.py --release 6.18.52-ophub` passes on the assembled
image. Board-tested: SSH over Wi-Fi, `/proc/device-tree/model`
`ZTE W103D`, cold/warm reboot, `uname -r` vs vermagic strict match.
Known carried boundary: scan-period packet loss unresolved.
